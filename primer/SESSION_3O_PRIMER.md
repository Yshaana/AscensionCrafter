# SESSION `3O` PRIMER — delivery modelling opens, the one-button capture, and `3n`'s last mile

> **`SUPERSEDED BY primer/Session_2026-08-08_3o_delivery.md`** — the work order
> for session `3o`, drafted 2026-08-08 (evening) from `AUDIT_3N_ADVERSARIAL.md`
> (§5 was the source list). `3o` closed 2026-08-08; read the record for what
> actually happened, including the two blocks whose mechanisms were measured and
> **refused** rather than modelled, and the one audit finding (F4) that did not
> survive contact with the tree. Marked in the close-out commit, per the rule.

**Audit implemented:** `primer/AUDIT_3N_ADVERSARIAL.md` (landed at `3o` pre-flight).
**Previous session record:** `primer/Session_2026-08-08_3n_clocks.md`.
**Monitoring primer:** `primer/CHAT_MONITORING_PRIMER.md` (v10, written with the audit).
**Addon spec:** `primer/ADDON_SPEC_ace_session_capture.md` (owner decisions already taken).

---

## §0 — The rules

🛑 **`3o` is the DELIVERY MODELLING session.** Absent mass is **59.0%** of cohort
logged damage and dominated by trigger-delivered damage; it is the load-bearing
problem, everything else in this order is either its prerequisite or its protection.

1. **No commit that can move the gate lands without a pre-registration that is its
   commit-parent.** A falsified prediction is reported, not rescued. An unexpected
   move is a finding: stop, commit the pair with its cause, no retroactive prereg.
2. **A quoted baseline must exist as a COMMITTED artifact derived from the current
   tree.** Rebuild, regenerate, commit, *then* predict.
3. **No prereg may carry a one-sided falsifier.**
4. **Every mechanism ships with a registered mutation — run RED and GREEN**, and the
   mutation must be one someone might actually have written (M60's isolating pattern;
   M68's crash lesson; `3n`'s label lesson: **assert the magnitude the mechanism
   produces, never a label the code assigns to itself**). **M72+ are yours.**
5. **Publish the same-member number beside every published median — AND AT ITS OWN
   ENDPOINTS** (`AUDIT_3N` F3, new). A delta may only sit beside two medians if it
   spans exactly those two runs. `3n`'s close-out put a leg1→leg2 delta (−0.0562)
   beside baseline→close medians (0.3234 → 0.2571, which differ by −0.0663) and
   attached an "accuracy, not composition" claim to a span whose first segment was
   composition by its own scoring.
6. **No coefficient is fitted to the parse it must later check.** What you cannot
   build from provenanced data is a **named refusal**.
7. **Never quote coverage as progress toward the gate.**
8. 🆕 **A CORRECTION'S BLAST RADIUS INCLUDES ITS VALUE'S CODE SITES — grep for the
   VALUE, not the sentence** (`AUDIT_3N` F1/F2, and it is the session's headline
   lesson). `3n` retired the "+88 AP per imbue" in both seeds and its commit said
   "and its blast radius" — while `core/sim/buffs.py:81` still executes
   `attack_power=88.0` inside the gate pipeline, and an open question in a file the
   pre-flight edited still cites the doubly-retracted `(9+AP)×2.2` as "the corrected
   formula". Prose siblings are now found reliably; **code constants are the siblings
   still slipping through.** When a magnitude is corrected: `grep -rn "88\|1\.21\|9+AP"`
   across `core/ tools/ ingest/ primer/ builds/` before the commit.
9. **A hand tally is a magnitude with no owner** (`AUDIT_3M` F3, recurred as
   `AUDIT_3N` F4/F6: "27.4" stale in the closing table, "105 → 94" reproducible from
   no committed artifact). If a number belongs in the close-out, a tool prints it.
10. **The holdout stays unspent** unless the owner explicitly spends it.
11. **THE MONDAY SPLIT IS LIVE FROM 2026-08-10.** The frozen cohort stays pre-fix.
    The owner holds Improved Cleave **0/3** — his own captures cannot be contaminated;
    the split bites only **card-holding cohort members'** post-Monday parses (Blix,
    Lootgoblin, Robottikyrpa, Nodding). E1 must land before any such parse is
    *compared* to anything pre-fix (Block G).

---

## Pre-flight — cannot move the gate; one commit (plus its own for anything that
touches a check)

🛑 **The `buffs.py` 88 is NOT in this list — it can move the gate. It is Block C.**

1. **Land the cycle documents**: `primer/AUDIT_3N_ADVERSARIAL.md` (born `FINDING`),
   the v10 monitoring primer (in place — never a versioned sibling), this file, and
   `primer/ADDON_SPEC_ace_session_capture.md` — **regenerate the census in the same
   commit** or `[A4]` fails at your first commit.
2. **`AUDIT_3N` F2** — `seed_epistemics.py`, `improved_cleave_lc_share_remeasure`:
   remove "the corrected (9+AP)x2.2 bonus-term formula (v9) predicts a large gain" —
   both halves are retracted. The question itself stays open, reframed post-Monday:
   re-measure LC's share once IC is acquired, at bonus-term-only **74.4/hit**.
3. **`AUDIT_3N` F3** — `PROGRESS.md` top block: re-quote the producing pair at its
   own endpoints (the −0.0562 = −0.0562 pair is **leg 1 → leg 2**, 0.3133 → 0.2571;
   baseline→close is 0.3234 → 0.2571 and its first segment is the P4b composition
   move, correctly). Same pass in the `3n` record: **append**, don't rewrite (it is
   `HISTORICAL`).
4. **`AUDIT_3N` F4** — the admissible-only slice has NO emitter and the closing
   table's 27.4 is stale (final manifest gives **27.62, n=21**). Emit it — manifest
   key or `render_band_table.py` row, asserted like the others — and paste the
   emitted value everywhere 27.4 appears as current.
5. **`AUDIT_3N` F6** — "starved allocations 105 → 94" reproduces from no committed
   artifact (the committed counts are 37→27 keyed-at-zero, 28→22 per-key starved).
   Either a tool prints the 105/94 derivation, or the sentence quotes the committed
   counts instead.
6. **`AUDIT_3N` §2** — the Devour Mind registration says the trigger-walk result "is
   not recorded anywhere"; its sibling row
   (`unmodelled_damage_is_unreachable_ids_not_missing_numbers`) records the worked
   case. Reconcile in one sentence: **no trigger edge reaches 287865** — the plan's
   step (1) is answered; the pressing card is the open question.
7. Rebuild after seed edits; `git status --short` before every commit.

## Block A — baseline (short: the instruments exist)

Rebuild `builds.db`, regenerate `gate_manifest_3e.json` and
`per_ability_summary.json`, commit. Expected unchanged from `3n`'s close:
`0 / 0 / 30.150 (n=24)`, producing 0.2571 (n=123), absent 59.0 — **if anything
moved, stop: that is a finding, not a baseline.** Cite the clean-tree `[arms]` line.

## Block B — the one-button capture (early, by owner decision — he wants it for his
next dungeon)

Implement `primer/ADDON_SPEC_ace_session_capture.md` exactly; the owner decisions
(auto-toggle logging; combat start+end snapshots only) are already stamped in it.
Gate-neutral by construction. In the same block, **generalise
`tools/analysis/pair_parses_to_stats.py`**: any capture folder, N blocks, and 🛑
**derive capture gaps from the log files' own first/last event stamps with
milliseconds** (`AUDIT_3N` F7 — the hand-typed whole-second constant makes Gehennas
read 319.8 where the bytes give ~320.0, and `3m` published 320.1 from the same
bytes; expect the number to move and say so where 319.8 is quoted). Parser-tolerance
check arm + mutation (a parser that chokes on the new `SnapshotReason:`/`Target:`
lines). Acceptance = the spec's live smoke test, owner on a dummy.

## Block C — the `3n` blast-radius correctness pair (own prereg; CAN move the gate)

`core/sim/buffs.py` `CONSECRATED_WEAPON_IMBUE.attack_power` 88.0 → the measured
**80** (or the decoded raw **73** with the named **×1.096**, whichever the code
expresses more honestly — evidence: the 2026-08-08 three-step capture). This
executes in the gate pipeline via `group_buffs.derive_buffs` →
`calibrate_crawled.py:1042` for any cohort member whose board holds card 200809.
**Prereg first:** query how many cohort members actually get the buff derived,
predict per-member direction (holders' sim damage falls slightly; non-holders do not
move at all), slice pair published + same-member. If zero members derive it, predict
exactly that — no movement is also falsifiable. Mutation (M72): restore 88.0 —
needs an arm that asserts the buff magnitude against the seeded/capture value, red
under the restore, green now. **Then grep the tree for `88` in imbue context one
more time — rule 8.**

## Block D — protect Block B of `3n` (the F5 fixture gap) before building on it

Every fixture holds exactly ONE next-swing ability, so the audit ran two mutants the
harness cannot see: the shared-budget decrement deleted (two Cleaves riding one
white swing) and leg 2 reducing the OFF hand — **both stayed green**. Add one
fixture with **two next-swing abilities of distinct per-swing damage** (a dual-wield
board), arms asserting: joint consumption ≤ the swing budget, allocation order
highest-damage-per-swing first, `auto_oh` untouched by replacement. Mutations
(M73: decrement deleted; M74: off-hand reduced) — run red/green. Cheap, and it
protects the arc's biggest modelling fix while Block E builds on top of it.

## Block E — 🚨 DELIVERY MODELLING (the headline; own prereg per mechanism)

Absent mass **59.0%**, dominated by trigger-delivered damage. Open with the two
items whose formulas are **stated by the client**, then the measured proc:

1. **Imbue per-hit** — effect 0 of the enchant-delivered 200819–200827 chain,
   divisors **77/25 stated in the tooltip template**, rank per character via
   `snapshot_gear.enchant_id` (the addon's weapon itemStrings carry this per parse
   going forward). Model the **+73 AP per weapon grant** at the same time (seeded
   `3m` C3, re-measured `3n`, modelled never — Block C is its prerequisite, not a
   substitute). The 88-vs-80 delta and the sheet weapon-damage anomaly stay **named
   findings, not fitted**.
2. **Seal per-proc 20424** — 35% weapon, Holy; proc rate measured `2e` at ~0.25 per
   melee event from autos AND abilities (not autos-only — that reading was
   retracted). Delivery-blocked, not extraction-blocked.
3. **Devour Mind 287865** — per its registered plan, step (1) already answered
   (pre-flight item 6): no trigger edge reaches it, so identify the **pressing
   card** first. 🛑 The join is mechanical — never by name, never by id proximity
   (287860 vs 287865 is exactly the forbidden shape).

Per mechanism: own prereg (which absent keys convert, expected direction of the
absent share; do NOT predict the slice unless derivable), registered mutation,
symmetric falsifier. Expect the absent share to FALL and say by how much per
mechanism where computable. **Seed early — no seeding in the session's tail**
(standing owner precedent).

## Block F — the fresh measurements, scored (if E leaves room; else `3p` head)

1. **The ×1.141 LBC residual** — compute the sim's talent layer for the exact
   2026-08-08 board (IC 0/3, no imbue, known stats) and score observed 523.8 vs
   modelled. The candidate is the Holy-chain double-dip on the Holystrike hybrid;
   whatever the layer gives, the residual gets a number and an owner.
2. **The 88-vs-80 imbue AP delta** — successor question; the discriminator is what
   differed between the 2026-08-05 and 2026-08-08 boards (ask the owner, §stop-points).
3. The weapon-damage +15.4×speed sheet anomaly stays recorded-not-derived.

## Block G — E1, the date-aware impairment (land, or register its trigger)

Apply the pre-2026-08-10 whole-ability Improved Cleave behaviour only to parses
dated before the fix (`SYSTEM_IMPAIRMENTS` shape with `fixed_on`). **Required before
the first post-Monday parse of a card-holding cohort member is compared to anything
pre-fix** — and that parse is also the free fix-shipped detector. The daily crawler
will pull post-Monday parses on its own schedule; if E1 does not land in `3o`, a
registered row must state the trigger ("before any card-holder comparison") so it
cannot be silently skipped. The owner's own post-Monday control capture should show
**no change** (IC 0/3) — that tests the changelog's "Regular Cleave was unaffected".

---

## Standing decisions needed from the owner — ask at session START

1. **Delivery scope:** imbue + seal + Devour Mind identification is the committed
   floor. If room remains: Plague Swarm 276445 (146×) or school-variant autos next?
2. **E1 in `3o`, or trigger-registered?** (Post-Monday card-holder crawl parses
   arrive on the crawler's schedule either way.)
3. **The 88-vs-80 question:** what changed on the board between 2026-08-05 and
   2026-08-08 — any Deadliness-adjacent card, path state, or buff present then?
4. **Addon smoke test:** dummy test right after Block B lands, or first real dungeon?
5. **Holdout:** unspent by default. **Predicate 2:** UNARMED by default.

## Close-out (its own commit, per the established pattern)

Session record (born `HISTORICAL`) with the §0 commit-by-commit gate table and
prereg→code parent pairs; per-block prediction counts read from the scored tables;
`PROGRESS.md` top block replaced, `3n` block collapsed; this file marked
`SUPERSEDED BY` the record; final artifacts from a clean tree; all three harnesses
cited (clean-tree `[arms]` line); census regenerated in every `primer/` commit;
**every published median beside its same-member pair, at matching endpoints**; the
honest NOT-done list. 🆕 **Before the close-out commit: grep the tree for every
magnitude this session corrected — the value, not the sentence.**

## What `3o` should hand to `3p`

Delivery's first two mechanisms modelled with mutations behind them and the absent
share measurably down; the imbue AP constant correct in the one place it executes;
the owner capturing bracketed dungeon sessions with one button; `3n`'s clock fix
protected against the two mutants the audit ran; Devour Mind's pressing card
identified or its absence proven; the Monday split handled by date or by a
registered trigger; and a close-out whose every number a tool printed.
