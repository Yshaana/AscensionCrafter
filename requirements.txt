# AscensionCrafter dependencies. Deliberately short — most of this repo is stdlib,
# and the ingest/rebuild chain (py cli/rebuild.py) needs NOTHING from this file.
#
#   pip install -r requirements.txt

# Acquisition only: tools/scrapers/* (the daily crawler, changelog fetcher, scouting).
requests

# Phase 1 Task 5's relationship graph: core/spells/graph.py.
# ARCHITECTURE §T5 - "using networkx, no graph database; at a few thousand nodes this
# is trivially in-memory and a graph DB adds an operational dependency for no gain."
networkx

# Phase 1 Task 9 browsing (py cli/browse.py + tools/browse/datasette_metadata.json).
# Approved as a pinned dependency by owner decision 2026-08-05 (was "install ad hoc").
datasette

# NOT a dependency, on purpose:
#   * StormLib - ingest/dbc/build_dbc_index.py loads it via ctypes from a locally
#     built DLL (ASCENSION_STORMLIB_DLL overrides the path). Only needed when
#     re-extracting from the game client; every other session reads the committed
#     JSON in data/source/dbc/ instead.

# OPTIONAL, and only for tools/audit/check_addon_lua.py (`3o` Block B): a real
# Lua 5.1 runtime, which is the version the 3.3.5 client runs. It lets the
# capture addon be LOADED AND DRIVEN in a test — slash commands dispatched,
# combat events fired, the session blob handed to the same parser the repo uses
# on real captures — instead of being verified only by the owner installing it
# and playing. 🛑 NOT needed by cli/rebuild.py, and NOT needed by
# check_refusals.py: the routine harness must stay green on a bare checkout, so
# the parser-tolerance arm that gates every run ([3o-B1]) uses no Lua at all.
lupa
